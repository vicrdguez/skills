Status: fix_required

Refused: no skl configuration exists at /tmp/test/011/skl/config.json

Repair: create /tmp/test/011/skl/config.json as {"ledger": "/absolute/path/to/ledger-clone"} pointing at an existing local Git clone of the ledger

No Claim was acquired. Make the repair, then rerun:

`skl implement next --repo '/work/widgets'`
