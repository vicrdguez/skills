Status: interrupted

The wait for Watchdog Review work was interrupted, and a Claim may have been acquired before it stopped. Run `skl status --repo '/work/widgets' --remote 'origin'` to see which Work Items hold a Claim. Tell the user the wait was interrupted and what that shows, and stop.
queue waiting interrupted: context canceled
