The implement Execution Skill arrives with selected work, and this retrieval acquired no Claim. Run `skl implement next` to start one Work Item, or `skl implement resume --item <proposal>/<slice> --claim <acquisition-commit>` to continue a Claim you hold. Its named resources render with `skl skill --resource <name> implement`.
