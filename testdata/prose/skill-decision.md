# Decision Inbox

Run `skl decision inbox`. It lists every current Needs Human request with the rules for answering it. Add `--project <name>` only when the user names a Project.

The rules alone, without requests: `skl skill --resource triage.md decision`.
