# Tasks - install-simplicity-standard

## Behavioral

- [ ] B1 Fresh Setup installs the approved standard by default -> behavior.md, first scenario.
- [ ] B2 Setup refreshes owned guidance without rewriting user guidance -> behavior.md, second scenario.
- [ ] B3 Repeating Setup does not duplicate or change the standard -> behavior.md, third scenario.
- [ ] B4 Direct Audit retrieval preserves ordinary review without Ponytail -> behavior.md, first Scenario Outline; cover Markdown and JSON.
- [ ] B5 Implementation packets bundle the same Audit without a new simplicity dependency -> behavior.md, second Scenario Outline; cover Markdown and JSON.

## Chores

- [ ] C1 Verify that the implementation changes Audit only as enumerated in plan.md and changes no other Skill Definition or resource, using the post-#8 Target Snapshot as the comparison base. Retain existing regression checks and complete the existing Audit and Full Gate.

## Docs

- [ ] D1 Update docs/capabilities/consumer-repository-setup.md to describe the standard as delivered rather than planned, preserving the explicit exclusions.
