# Tasks - install-simplicity-standard

## Behavioral

- [x] B1 Fresh Setup installs the approved standard by default -> behavior.md, first scenario.
- [x] B2 Setup refreshes owned guidance without rewriting user guidance -> behavior.md, second scenario.
- [x] B3 Repeating Setup does not duplicate or change the standard -> behavior.md, third scenario.
- [x] B4 Direct Audit retrieval preserves ordinary review without Ponytail -> behavior.md, first Scenario Outline; cover Markdown and JSON.
- [x] B5 Implementation packets bundle the same Audit without a new simplicity dependency -> behavior.md, second Scenario Outline; cover Markdown and JSON.

## Chores

- [x] C1 Verify that the implementation changes Audit only as enumerated in plan.md and changes no other Skill Definition or resource, using the post-#8 Target Snapshot as the comparison base. Retain existing regression checks and complete the existing Audit and Full Gate.

## Docs

- [x] D1 Update docs/capabilities/consumer-repository-setup.md to describe the standard as delivered rather than planned, preserving the explicit exclusions.
