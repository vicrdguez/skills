# Tasks - Drain Codex Queues

Approved slice 5. Blocked by Merged `drain-pi-queues`, not OpenCode. Behavioral cycles test delivered CLI instructions and installed native configuration, not live LLM compliance.

## Behavioral

- [ ] B1 Discover loop skills separately from one-item skills -> behavior.md B1; existing temporary-home installation and CLI retrieval seam.
- [ ] B2 Deliver fresh one-worker-per-lane native dispatch instructions -> behavior.md B2; retrieved Codex adapter instructions and installed startup contract.
- [ ] B3 Deliver authoritative continuation and bounded waiting instructions -> behavior.md B3; retrieval checks against predecessor command contract, without a new scheduler.
- [ ] B4 Deliver safe stop and recovery boundaries -> behavior.md B4; retrieved guidance for timeout, errors, incomplete handoffs, and ambiguous selection.
- [ ] B5 Deliver nested independent Audit axes -> behavior.md B5; implementation/Audit packet and installed native role instruction checks for complete supplied axis briefs, recorded checks, and explicit resource pointers, matching Pi without a new axis retrieval interface or recursive Audit invocation.
- [ ] B6 Install complete thin Codex roles with explicit defaults -> behavior.md B6; existing CLI install tests and parsed installed TOML.
- [ ] B7 Refresh managed global defaults while preserving repository overrides -> behavior.md B7; temporary-home ownership-marker refresh, repeat install, unowned conflict reporting, and byte-preservation checks for repository overrides and unrelated configuration.
- [ ] B8 Deliver actionable prerequisite failures without fallback -> behavior.md B8; installed/retrieved prerequisite and failure instruction checks.
- [ ] B9 Document complete trusted-project native overrides -> behavior.md B9; parse copyable README templates and check startup references and override/trust guidance.

## Chores

- [ ] C1 Reuse confirmed shared roles `skl-implement`, `skl-watchdog`, `skl-audit-standards`, `skl-audit-artifacts` and skills `implement-loop`/`watchdog-loop`; read the merged predecessor's ownership policy, retrieval paths, waiting syntax, and authoritative startup/continuation interface before coding, without an OpenCode dependency.
- [ ] C2 Verify and record supported installed Codex version semantics for native role loading, complete-file project overrides, fresh new-session spawning without parent-history forks, and nested Audit capacity using official documentation and available schema/version evidence. Halt on an unsupported prerequisite; no invented knobs or live harness smoke tests.
- [ ] C3 Wire only needed Codex managed global roles/stubs/resources into existing embedding and ownership-marker installation; preserve and report unowned conflicts and leave unrelated harness behavior, supervisor configuration, and native repository overrides untouched. Add no field-aware global edit preservation or configuration rewriting feature.
- [ ] C4 Run focused install/retrieval/parser checks and the repository Full Gate, including `go test ./...` and `git diff --check` plus any other documented checks. Record these as deterministic delivery checks, not proof of live agent judgment or context isolation.

## Docs

- [ ] D1 Update README with native loop entry points, two-session usage, inherited configurable waiting, uncapped draining, explicit worker defaults, prerequisite/remedy guidance, and complete copyable project role templates for all four roles. Cite official Codex subagents/config docs; explain complete-file replacement, project trust, ownership, and optional model diversity without superiority claims.
- [ ] D2 Update `docs/capabilities/skill-distribution.md` and `docs/capabilities/work-item-lifecycle.md` in capability format to describe delivered Codex integration and configuration boundaries, preserving shared engine authority and the human merge boundary. These are the final documentation updates.
